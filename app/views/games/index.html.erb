<div class="container">
  <div class="notification is-primary">
    <%= paginate @games %>
      <div class="columns is-multiline is-mobile" class=container>
      <%= render partial: 'game', collection: @games, locals: {show_link: true} %>
    <%= paginate @games %>
    </div>
  </div>
</div>

